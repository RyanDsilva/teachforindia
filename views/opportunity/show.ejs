<<<<<<< HEAD
<% include ../partials/header %>
    <div class="container show">
        <div class="row">
            <div class="col-md-3">
                <p class="lead">Apply As Volunteers</p>
                <div class="list-group nav-opts">
                    <li class="list-group-item active">
                        <a href="#title" id="title-nav"><%=opportunity.title%></a>
                    </li>
                    <li class="list-group-item"><a href="#details">Details</a></li>
                    <li class="list-group-item"><a href="#apply">Apply</a></li>
                </div>
            </div>
            <br>
            <div class="col-md-9">
                <div class="card">
                    <div class="card-body">
                        <h4 class="float-right">
                            <%=opportunity.city%>
                        </h4>
                        <h4 id="title">
                            <a>
                                <%=opportunity.title%>
                            </a>
                        </h4>
                        <p>
                            <%- opportunity.description %>
                        </p>
                        <p id="details" >
                            <Address>
                                <strong>Address:</strong>
                                <%= opportunity.address %>
                            </Address>
                        </p>
                        <p>
                            <strong>Duration: </strong>
                            <%= opportunity.duration %>
                        </p>
                        <p>
                            <strong>Date: </strong>
                            <%= opportunity.date.toDateString() %>
                        </p>
                        <!-- AUTH LOGIC -->
                        <% if (currentUser && opportunity.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-warning" href="/opportunity/<%= opportunity._id %>/edit">Edit</a>
                            <% } %>
                    </div>
                </div>
                <br>
                <br>
                <div class="card card-body bg-light">
                    <div class="text-right">
                        <a id="apply" class="btn btn-success" href="/opportunity/<%= opportunity._id %>/application/new">Apply</a>
                    </div>
                    <hr>
                    <!-- TODO: Implement Applications Show 
                    <% opportunity.applications.forEach(function(application){ %>
                    <div class="row">
                        <div class="col-md-12">
                            <strong><%= application.author.name %></strong>
                            <span class="text-right"><%= application.email %></span>
                            <p>
                                <%= application.description %>
                            </p>
                            <p>
                                <%= application.experience %>
                            </p>
                            <% if(currentUser && application.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-warning btn-sm" href="/opportunity/<%= opportunity._id %>/application/<%= application._id %>/edit">Edit</a>
                            <form action="/opportunity/<%= opportunity._id %>/application/<%= application._id %>?_method=DELETE" method="post" class="del-btn">
                                <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                            </form>
                            <% } %>
                        </div>
                    </div>
                    <% }); %> -->
                </div>
            </div>
        </div>
    </div>

=======
<% include ../partials/header %>
    <div class="container show">
        <div class="row">
            <div class="col-md-3">
                <p class="lead">Apply As Volunteers</p>
                <div class="list-group">
                    <li class="list-group-item active">
                        <%=opportunity.title%>
                    </li>
                    <li class="list-group-item">Details</li>
                    <li class="list-group-item">Apply</li>
                </div>
            </div>
            <div class="col-md-9">
                <div class="card">
                    <div class="card-body">
                        <h4 class="float-right">
                            <%=opportunity.city%>
                        </h4>
                        <h4>
                            <a>
                                <%=opportunity.title%>
                            </a>
                        </h4>
                        <p>
                            <%- opportunity.description %>
                        </p>
                        <p>
                            <Address>
                                <strong>Address:</strong>
                                <%= opportunity.address %>
                            </Address>
                        </p>
                        <p>
                            <strong>Duration: </strong>
                            <%= opportunity.duration %>
                        </p>
                        <p>
                            <strong>Date: </strong>
                            <%= opportunity.date.toDateString() %>
                        </p>
                        <!-- AUTH LOGIC -->
                        <% if (currentUser && opportunity.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-warning" href="/opportunity/<%= opportunity._id %>/edit">Edit</a>
                            <% } %>
                    </div>
                </div>
                <br>
                <br>
                <div class="card card-body bg-light">
                    <div class="text-right">
                        <a class="btn btn-success" href="/opportunity/<%= opportunity._id %>/application/new">Apply</a>
                    </div>
                    <hr>
                    <!-- TODO: Implement Applications Show 
                    <% opportunity.applications.forEach(function(application){ %>
                    <div class="row">
                        <div class="col-md-12">
                            <strong><%= application.author.name %></strong>
                            <span class="text-right"><%= application.email %></span>
                            <p>
                                <%= application.description %>
                            </p>
                            <p>
                                <%= application.experience %>
                            </p>
                            <% if(currentUser && application.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-warning btn-sm" href="/opportunity/<%= opportunity._id %>/application/<%= application._id %>/edit">Edit</a>
                            <form action="/opportunity/<%= opportunity._id %>/application/<%= application._id %>?_method=DELETE" method="post" class="del-btn">
                                <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                            </form>
                            <% } %>
                        </div>
                    </div>
                    <% }); %> -->
                </div>
            </div>
        </div>
    </div>

>>>>>>> 155441e18bdbfcc2fd483b73e65bbfda087dc39d
    <% include ../partials/footer %>